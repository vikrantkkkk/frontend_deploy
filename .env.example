# API base URL (no trailing slash).
# Dev with Vite proxy: leave empty — requests use /api/users and proxy to the backend.
# Dev direct / production: set full backend URL, e.g. http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000
